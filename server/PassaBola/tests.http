@host = http://localhost:8000
@email = alice@example.com
@password = passw0rd

### 1) Signup
POST {{host}}/auth/signup
Content-Type: application/json

{
  "name": "Alice",
  "email": "{{email}}",
  "password": "{{password}}",
  "phone_number": "123456789",
  "document": "ABC123",
  "birth_date": "1990-01-01"
}

### 2) Login e captura do token
# @name login
POST {{host}}/auth/login
Content-Type: application/json

{
  "email": "{{email}}",
  "password": "{{password}}"
}

### 3) Call a protected route with the token (should return current user)
@token = token
GET {{host}}/auth/me
Authorization: Bearer {{token}}

### 4) Negative test: protected route without token (should be 401)
GET {{host}}/auth/me
